<UserControl x:Class="SuperDevFact.Desktop.Views.Invoices.PaymentListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:SuperDevFact.Desktop.Converters"
             mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <conv:PaymentMethodLabelConverter x:Key="PaymentMethodLabelConverter" />
    </UserControl.Resources>

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <ScrollViewer Padding="28">
            <StackPanel>
                <TextBlock Text="Paiements" Style="{StaticResource DisplayText}" Margin="0,0,0,4" />
                <TextBlock Text="Tous les encaissements enregistrés." Style="{StaticResource SecondaryText}" Margin="0,0,0,20" />

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="130" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="N° facture" Style="{StaticResource CaptionText}" />
                            <TextBlock Grid.Column="1" Text="Client" Style="{StaticResource CaptionText}" />
                            <TextBlock Grid.Column="2" Text="Date" Style="{StaticResource CaptionText}" />
                            <TextBlock Grid.Column="3" Text="Montant" Style="{StaticResource CaptionText}" />
                            <TextBlock Grid.Column="4" Text="Moyen de paiement" Style="{StaticResource CaptionText}" />
                        </Grid>

                        <ItemsControl ItemsSource="{Binding Payments}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,12" Cursor="Hand">
                                        <Border.InputBindings>
                                            <MouseBinding MouseAction="LeftClick"
                                                          Command="{Binding DataContext.SelectPayment, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                          CommandParameter="{Binding}" />
                                        </Border.InputBindings>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="120" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="120" />
                                                <ColumnDefinition Width="120" />
                                                <ColumnDefinition Width="130" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding InvoiceNumber}" Style="{StaticResource BodyText}" FontWeight="SemiBold" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="1" Text="{Binding CustomerName}" Style="{StaticResource BodyText}" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="2" Text="{Binding PaymentDate, StringFormat='{}{0:dd/MM/yyyy}'}" Style="{StaticResource SecondaryText}" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="3" Text="{Binding Amount, StringFormat='{}{0:N2} €'}" Style="{StaticResource BodyText}" FontWeight="SemiBold" Foreground="{StaticResource SuccessBrush}" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="4" Text="{Binding Method, Converter={StaticResource PaymentMethodLabelConverter}}" Style="{StaticResource SecondaryText}" VerticalAlignment="Center" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
